import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        // primary: {main: "#fcca46"},
        secondary: {main: "#0f4c5c"},
        mode: "dark"
      },
  });

export default theme;
  